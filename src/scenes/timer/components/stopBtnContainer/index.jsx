import React from 'react';

import {
    Btn,
} from '../../../../components';

function StopBtnContainer() {
    return <Btn callback={() => {}} text={'Stop'} />;
}

export default StopBtnContainer;
